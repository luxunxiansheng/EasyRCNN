FASTER_RCNN:
  FEATRUE_EXTRACTOR: 'pretrained_vgg16'
  
  TRAIN:
    CHECK_FREQUENCY: 100
    EPOCHS: 50
    NUM_WORKERS: 2
    PRETRAINED_MODEL_PATH: None
    RESUME: False


VOC_DATASET:
  AUGMENTED: False

CHECKPOINT:
  CHECKPOINT_PATH: '/media/yan/D/ornot/workspace/object_detection/checkpoint/exp04/model_checkpoint.ckpt'

LOG:
  LOG_DIR: '/media/yan/D/ornot/workspace/object_detection/log/exp04'



